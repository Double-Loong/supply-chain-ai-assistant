
prd_content = """# 智链AI - 供应链智能预测助手
## 产品需求文档（PRD）

---

**文档版本**：V1.0  
**撰写日期**：2025年5月  
**撰写人**：李龙龙  
**产品定位**：基于AI LightGBM的SKU级需求预测与智能补货决策系统  
**目标用户**：供应链主计划员、库存管理员、采购专员  

---

## 一、项目背景

### 1.1 业务痛点

在Lotto实习进行sku库存管理和在京东进行sku日常运营时，我发现需求分析库存管理面临以下核心痛点：

| 痛点 | 具体表现 | 业务影响 |
|-----|---------|---------|
| 预测依赖人工经验 | 许多工作用Excel手工调整，促销期预测偏差大 | 缺货损失/库存积压 |
| 数据分散 | 订单、库存、用户数据在不同系统，难以关联分析 | 决策滞后 |
| 补货决策慢 | 客户下单反馈需求后才执行缺货sku补货 | 周转效率低 |
| 缺乏量化评估 | 没有形成可预测的补货方式 | 缺乏迭代优化 |

### 1.2 项目目标

**一句话目标**：构建AI需求预测原型，验证"大样本数据驱动+AI算法"替代"人工经验"的可行性，并输出可落地的产品方案。

**量化目标**：
- AI模型MAPE ≤ 20%（基线：移动平均MAPE 25.3%）
- 支持Top 50 SKU的7天/14天/30天预测
- 补货建议生成时间从"天级"降至"小时级"

---

## 二、用户画像

### 2.1 核心用户：主计划员 小王

| 维度 | 描述 |
|-----|------|
| 背景 | 25-35岁，供应链/物流专业，1-2年经验 |
| 日常工作 | 每周一上午编制下周MPS主生产计划，调整SKU补货量 |
| 核心诉求 | 希望有一个工具能快速告诉我"下周哪些SKU可能缺货，建议补多少" |
| 痛点场景 | 大促前一周，老板问"iPad Air备了多少货"，我只能凭感觉回答"应该够" |
| 技术能力 | Excel熟练，会VLOOKUP和数据透视表，不懂Python与sql |

### 2.2 次要用户：库存管理员 李姐

| 维度 | 描述 |
|-----|------|
| 背景 | 40岁+，仓库管理10年经验，对系统操作敏感 |
| 核心诉求 | 每天早上一眼看懂"今天哪些货要到了，哪些货快没了" |
| 痛点场景 | 系统预警不及时，经常发现缺货时已经影响发货了 |

---

## 三、产品架构

### 3.1 信息架构

```
智链AI
├── 登录页
│   ├── 系统名称（智链AI 供应链智能预测助手）
│   ├── 公司标识（公司简称与LOGO）
│   ├── 用户名（企业内部工号）
│   └── 密码（企业统一身份密码标识）

├── 数据概览（首页）
│   ├── KPI看板（今日销量/本周预测准确率/预警SKU/AI建议采纳率）
│   ├── 销量趋势图（近30天+未来7天预测）
│   ├── 库存预警列表（红色/黄色标记）
│   └── 品类销量分布柱状图
│
├── 预测分析（核心功能）
│   ├── SKU选择器（品类/sku）
│   ├── 时间范围控制（7天/14天/30天）
│   ├── 销量趋势与预测图（历史实际 vs AI预测 vs 移动平均）
│   ├── 预测详情表（日期/AI预测/传统法/实际值）
│   ├── AI洞察提示（自然语言解读）
│   ├──TOP5销量影响因素（TOP5特征重要性）
│   └── 对应营销建议（自然语言解读）
│
├── 补货建议（业务闭环）
│   ├── SKU选择器（品类/sku）
│   ├── 参数设置（当前库存/安全库存天数/预测周期）
│   ├── 建议补货量（大数字突出显示）
│   ├── 生成采购单按钮（模拟）
│   ├── 库存预警提示（条件触发）
│   └── 补货优先级矩阵（四象限）
│
└── 模型报告（技术说服力）
    ├── 预测区间选择（品类/预测时间）
    ├── 预测准确率对比（柱状图）
    ├── 关键指标表（MAE/RMSE/MAPE）
    ├── 特征重要性（Top 5）
    ├── 提升幅度卡片（37.5%）
    └── 技术栈说明
```

### 3.2 技术架构

```
数据源层
├── 订单数据（40万条）
│   ├── 字段：order_id, user_id, product_id, order_time, quantity, amount
│   └── 来源：网络开源电商数据
├── 用户数据（6万条）
│   ├── 字段：user_id, gender, age, register_time, total_purchase_times
│   └── 来源：网络开源电商数据
│
数据处理层（Python + pandas）
├── 数据清洗：仅保留已下单数据、缺失值处理、异常值过滤、时间格式统一
├── 特征工程：
│   ├── 时序特征：lag_1/3/7/14/30, rolling_mean_7/14/30
│   ├── 时间特征：dayofweek, is_weekend, is_month_start
│   └── 用户特征：用户分层（新客/低频/中频/高频）
└── 数据集划分：按时间顺序前80%训练，后20%验证
│
模型层（LightGBM）
├── 基线模型：7日移动平均、历史同期法
├── AI模型：LightGBM梯度提升树
│   ├── 参数：num_leaves=31, learning_rate=0.05
│   └── 特征数：30+维
└── 评估指标：MAE, RMSE, MAPE
│
应用层（Streamlit + Plotly）
├── 前端界面：交互式仪表盘
├── 可视化：趋势图、柱状图、表格、进度条
└── 部署：Streamlit Cloud（免费版）
```

---

## 四、功能详述

### 4.1 数据概览页

**页面目的**：让用户3秒内了解全局状态

| 元素 | 说明 | 数据来源 |
|-----|------|---------|
| KPI卡片×4 | 今日总销量/本周预测准确率/库存预警SKU数/AI建议采纳率 | 实时计算 |
| 销量趋势图 | 折线图，X轴日期，Y轴销量 | 近30日聚合表 |
| 库存预警列表 | 红色（库存<3天）、黄色（库存3-7天）| 显示SKU+预警类型 |
| 品类分布 | 横向柱状图，Top 5品类累计销量占比 | 品类聚合表 |

**交互规则**：
- 趋势图支持鼠标悬停显示具体数值
- 预警列表点击可跳转至该SKU的预测分析页
- 数据每小时自动刷新（模拟）

### 4.2 预测分析页

**页面目的**：核心功能，展示AI预测能力

**核心交互流程**：
```
用户选择SKU → 选择预测周期（默认7天）→ 点击"开始预测" 
→ 系统返回：趋势图 + 预测表格 + AI洞察 + TOP5销量影响因素 + 对应营销建议
```

| 元素 | 说明 | 设计细节 |
|-----|------|---------|
| SKU选择器 | 下拉搜索框，支持SKU编码和商品名称模糊搜索 | 输入时实时联想 |
| 时间范围 | 单选按钮：7天/14天/30天 | 默认7天，选中态灰色高亮 |
| 趋势图 | 三条线：历史实际（蓝实线）、AI预测（绿虚线）、移动平均（黄点线） | 预测区域浅绿背景标注 |
| 预测表格 | 日期/AI预测/传统法/实际值四列 | AI预测列蓝色加粗，实际值绿色高亮（AI更接近时） |
| AI洞察 | 自然语言总结，如"未来7天预计销量364件，建议关注5月10日至16日库存" | 蓝色信息框，位于图表下方 |

**边界情况**：
- 新SKU（历史数据<30天）：提示"数据不足，建议用移动平均"，禁用AI预测
- 缺货SKU：预测表格中实际值显示"缺货"，AI预测值用红色标注

### 4.3 补货建议页

**页面目的**：将预测转化为可执行的采购决策

**核心公式**：
```
建议补货量 = max(0, 预测总需求 + 安全库存 - 当前库存)
其中：安全库存 = 日均销量 × 安全库存天数
```

| 元素 | 说明 | 交互规则 |
|-----|------|---------|
| SKU选择器 | 下拉搜索栏，品类选择，支持SKU编码和商品名称模糊搜索 | 输入时实时联想 |
| 当前库存输入框 | 数字输入，默认从系统读取 | 用户可手动修改，验证≥0 |
| 安全库存天数滑块 | 范围3-14天，默认7天 | 滑动时实时更新建议补货量 |
| 预测周期选择 | 7天/14天/30天，与预测页联动 | 修改时重新计算 |
| 建议补货量 | 页面视觉中心，48px大数字 | >0 时红色（需补货），=0 时绿色（充足） |
| 生成采购单按钮 | 模拟按钮，点击弹出"采购单已生成"提示 | 实际对接ERP时需开发接口 |
| 库存预警提示 | 条件显示：当前库存<安全库存时触发 | 红色警告框，含具体天数和建议 |
| 补货优先级矩阵 | 四象限：高销量低库存（紧急）、高销量高库存（关注）、低销量低库存（常规）、低销量高库存（滞销） | 每个象限显示Top 3 SKU |

### 4.4 模型报告页

**页面目的**：向技术面试官/数据团队证明AI价值

| 元素 | 说明 | 设计意图 |
|-----|------|---------|
| 品类选择器 | 下拉搜索框，支持品类模糊搜索 | 输入时实时联想 |
| MAPE对比柱状图 | 三个柱子：LightGBM(15.8%)、历史同期(22.1%)、移动平均(25.3%) | 绿色最短，视觉冲击 |
| 关键指标对比 | 三行四列（模型/MAE/RMSE/MAPE） | LightGBM行绿色高亮 |
| 模型决策依据 | 横向进度条，Top 5特征 | 证明模型可解释性 |
| 模型提升效果 | 大数字"37.5%"，副标题"LightGBM vs 传统方法 预测误差降低" | 放在右侧，第一眼看到 |
| 技术栈说明 | 算法/数据/特征/训练方式 | 建立技术可信度 |

---

## 五、数据需求

### 5.1 输入数据

| 数据表 | 字段 | 用途 | 更新频率 |
|-------|------|------|---------|
| order.csv | order_id, user_id, product_id, order_time, quantity, amount, payment_method, promotion_type, order_status, shipping_city | 构建日维度销量表 | T+1 |
| user.csv | user_id, gender, age, register_time, register_channel, total_purchase_times, total_purchase_amount | 用户分层特征 | 月度 |
| product.csv | product_id, product_name, category, brand, price, launch_date | SKU基础信息 | 月度 |

### 5.2 输出数据

| 数据表 | 字段 | 用途 |
|-------|------|------|
| daily_sales | order_date, product_id, daily_qty, daily_amount | 模型输入 |
| features | 30+维特征 + target_7d | 模型训练 |
| predictions | order_date, product_id, actual, pred_lgb, pred_ma | 效果评估 |
| replenishment_suggestions | product_id, current_stock, safety_stock, suggested_order, priority | 补货决策 |

---

## 六、非功能需求

| 维度 | 要求 | 说明 |
|-----|------|------|
| 性能 | 预测响应<3秒 | 单SKU 7天预测 |
| 可用性 | 支持Chrome/Edge/Safari最新两版 | B端主流浏览器 |
| 安全 | 敏感数据不上传云端 | 本地运行，样本数据脱敏 |
| 扩展性 | 预留多SKU批量预测接口 | 未来支持全量SKU与连接在线数据库 |
| 可维护性 | 代码模块化，注释清晰 | 便于后续迭代 |

---

## 七、项目里程碑

| 阶段 | 时间 | 产出 | 状态 |
|-----|------|------|------|
| 数据清洗与EDA | Week 1 | 清洗后数据集 + 探索报告 | ✅ 完成 |
| 特征工程与建模 | Week 2-3 | LightGBM模型 + 对比报告 | ✅ 完成 |
| 产品化封装 | Week 4 | Streamlit仪表盘 + 墨刀原型 | ✅ 完成 |
| 文档与演示 | Week 5 | PRD + 演示视频 + 作品集 | 🔄 进行中 |

---

## 八、后续迭代规划

| 版本 | 功能 | 优先级 |
|-----|------|--------|
| V1.1 | 接入实时数据流，支持 T+N 自动预测 | P1 |
| V1.2 | 引入LLM生成自然语言补货建议报告 | P1 |
| V1.3 | 多仓协同预测（中央仓+区域仓） | P2 |
| V2.0 | 与ERP/WMS系统对接，实现一键下单 | P2 |
| V2.1 | A/B测试框架，持续优化模型效果 | P3 |

---

## 九、附录

### 9.1 关键术语

| 术语 | 解释 |
|-----|------|
| MAPE | 平均绝对百分比误差，衡量预测准确率的指标，越低越好 |
| RMSE | 均方根误差，对大偏差更敏感的误差指标 |
| LightGBM | 微软开源的梯度提升树框架，速度快、精度高 |
| SKU | 库存保有单位，即最小可管理商品单元 |
| 安全库存 | 为应对需求波动设置的缓冲库存 |

### 9.2 参考资料

- 《预测：方法与应用》（Spyros Makridakis）
- LightGBM官方文档：https://lightgbm.readthedocs.io/
- Streamlit官方文档：https://docs.streamlit.io/

### 9.3 原型预览

[此处插入5张原型图，或链接至原型文件夹]

- 登录页：[prototype_login.png]
- 数据概览仪表盘：[prototype_dashboard.png]
- 预测分析页：[prototype_prediction.png]
- 补货建议页：[prototype_replenishment.png]
- 模型效果报告页：[prototype_model_report.png]

---

**文档结束**

*本PRD为独立项目作品，用于展示供应链AI产品规划能力。*
"""
